version: '1.0'
steps:
  build:
    type: build
    image_name: codefresh/fs-ops
    working_directory: ./
    dockerfile: fs-ops/Dockerfile
    tag: latest 
  push:
    type: push
    candidate: ${{build}}
    tag: latest 
    registry: dockerhub