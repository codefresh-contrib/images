version: '1.0'
steps:
  build:
    type: build
    image_name: codefresh/kubectl
    working_directory: ./
    dockerfile: kubectl/Dockerfile
    tag: latest 
  push:
    type: push
    candidate: ${{build}}
    tag: latest 
    registry: dockerhub